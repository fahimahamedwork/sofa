server:
  port: 8080
  host: "0.0.0.0"

database:
  host: "localhost"
  port: 5432
  user: "sofa"
  password: "sofa"
  dbname: "sofa"
  sslmode: "disable"

redis:
  host: "localhost"
  port: 6379
  password: ""
  db: 0

auth:
  jwt_secret: "sofa-secret-change-in-production"
  admin_password: "admin"
  token_duration: "24h"

docker:
  socket_path: "/var/run/docker.sock"

traefik:
  api_url: "http://localhost:8080"
  base_domain: "localhost"
